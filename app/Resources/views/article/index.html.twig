{% extends 'base.html.twig' %}

{% block body %}
    Posts
    <br>
    <form method="get">

    </form>
    Recherche : <input type="text" name="q" value="">

    <br>
    <table>
    {% for post in posts %}
        <tr>
            <td>{{ post.id }}</td>
            <td>{{ post.title }}</td>
            <td>{{ post.createdAt|date('d-m-Y') }}</td>
        </tr>
    {% endfor %}
    </table>

{% endblock %}
